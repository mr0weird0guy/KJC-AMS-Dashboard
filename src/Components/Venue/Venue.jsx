const Venue = () => {
  const centerDiv = {
    height: "100vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  }

  return (
    <div style={centerDiv}>
      <h1>Venue</h1>
    </div>
  )
}

export default Venue
